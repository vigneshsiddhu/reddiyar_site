<%= form_for(@user_role) do |f| %>
  <% if @user_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_role.errors.count, "error") %> prohibited this user_role from being saved:</h2>

      <ul>
      <% @user_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :role %><br>
    <%#= f.collection_select :role, UserRole.roles, :role, :enum, {prompt: "Select a role"}, {class: "form-control input-lg"} %>
    <%= f.select(:role, UserRole.roles.keys.map {|role| [role.titleize,role]}) %>
    <%#= f.input :role, :as => :select, :collection => UserRole.roles.keys.to_a %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
